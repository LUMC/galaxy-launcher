SELECT pg_terminate_backend(pg_stat_activity.pid)
FROM pg_stat_activity
WHERE pg_stat_activity.datname = '{{container_database_name}}'
  AND pid <> pg_backend_pid(); 
DROP DATABASE {{container_database_name}} ;
CREATE DATABASE {{container_database_name}} WITH OWNER {{container_database_username}} ;

